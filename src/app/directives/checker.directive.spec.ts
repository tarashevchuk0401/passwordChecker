import { CheckerDirective } from './checker.directive';

describe('CheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckerDirective();
    expect(directive).toBeTruthy();
  });
});
